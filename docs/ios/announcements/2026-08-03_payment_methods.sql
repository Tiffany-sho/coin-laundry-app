-- =====================================================================
-- お知らせ: 支払方法ごとの記録（007 / 009）
--
-- ⚠️ **これはマイグレーションではない。** テーブルの形は変えず、
--    `announcements` に 1 行入れるだけ。何度実行しても増えない
--    （タイトルで NOT EXISTS を見ている）。
--
--   npx supabase link --project-ref <ref>
--   npx supabase db query --linked -f docs/ios/announcements/2026-08-03_payment_methods.sql
--
-- Supabase の Table Editor に手で貼ってもよい（そちらが本来の投稿口）。
-- その場合は category / title / body / published の 4 つを入れる。
-- =====================================================================

-- ---------------------------------------------------------------------
-- ⚠️ 文面を直すときに必ず守ること（005 の COMMENT と同じ内容）
--
-- ⚠️ **このお知らせは iOS アプリにもそのまま出る。** 出し分けの列は無い
--    （2026-07-30 の決定）。「Web で」と書かないのはこのため。
--    Web だけの話のつもりで書いても、アプリの設定画面に出る。
--
-- ⚠️ **価格・プラン名・外部サイトでの契約に触れない**（Guideline 3.1.3(a) / 3.1.2）。
--    書き出しは Pro 以上の機能だが、**プラン名を出さない。**
--    「Pro プランなら書き出せます」と書いた瞬間にリジェクト事由になる。
--
-- ⚠️ **アプリに無い画面へ誘導しない。** 下で挙げている 4 か所は
--    Web とアプリの両方に実在することを確認済み:
--      - 店舗の登録・編集フォームの「支払方法」
--        （Web: PaymentMethodForm.jsx / アプリ: PaymentMethodSection.tsx。
--         プリセットの 5 つも同じ）
--      - 集金入力の「キャッシュレス」欄と、機種別入力での設備ごとの内訳
--      - 収益ページの「支払方法別」グラフ・売上履歴の絞り込み
--      - データの書き出し（列が支払方法ごとに割れる）
--    ⚠️ **設定画面には支払方法の口が無い。** 店舗フォームの中だけなので、
--       「設定から登録できます」と書かないこと（実際に迷う）。
--
-- ⚠️ **Markdown は解釈されない**（Web / アプリとも `white-space: pre-wrap` で
--    そのまま出す）。記号で見出しを作るのはよいが、リンク記法を書いても
--    ただの文字列になる。
-- ---------------------------------------------------------------------

INSERT INTO public.announcements (category, title, body, published)
SELECT
  'feature',
  '支払方法ごとに集金を記録できるようになりました',
  -- 本文はドル引用符（$$…$$）で囲い、**改行をそのまま書く。**
  -- 005 の E'…\n' でも同じ結果になる（⚠️ 行を分けても E は続きの行にも効く。
  -- PGlite で確認済み）が、`\n` が並ぶと投稿前に文面を読めないので $$ にしてある。
  --
  -- ⚠️ **文字列定数を並べて連結するときは行を分けること。**
  --    `E'a\n' 'b'` と 1 行に並べると連結ではなく **42601（syntax error）**。
  --    連結には空白だけでなく**改行が要る**（実際に踏んだ）。
  $$現金以外の支払方法を店舗ごとに登録して、集金金額を方法ごとに分けて記録できるようになりました。

■ 店舗ごとに支払方法を登録する
店舗の登録・編集フォームに「支払方法」の欄が増えました。PayPay・クレジットカード・交通系ICなどはボタンから追加できます。受け付けている方法は店舗ごとに違ってかまいません。

■ 集金入力で方法ごとに金額を入れる
支払方法を登録した店舗では、集金入力に「キャッシュレス」の欄が出ます。機種別に入力しているときは、設備ごとに内訳を入れることもできます。
現金は登録しなくても常に記録されるので、これまでどおりの入力で問題ありません。

■ 売上を支払方法で見る
・収益ページに「支払方法別」のグラフが増えました
・月別売上と売上履歴を支払方法で絞り込めます
・データの書き出しでも、支払方法ごとに列が分かれます

一覧から外した支払方法も、過去の集金記録には名前と金額がそのまま残ります。
支払方法を 1 つも登録していない店舗の画面は、これまでと変わりません。$$,
  true
WHERE NOT EXISTS (
  SELECT 1 FROM public.announcements
  WHERE title = '支払方法ごとに集金を記録できるようになりました'
);

-- =====================================================================
-- 確認用
--
-- 1) 入ったか（1 行・published = true であること）
--      SELECT id, category, title, published, published_at
--        FROM public.announcements
--       ORDER BY published_at DESC;
--
-- 2) ⚠️ **実際に出るかはアプリ / Web で見る。** SQL Editor は service role なので
--    RLS を素通りする（下書きでも見えてしまい、確認にならない）。
--
-- 3) 下書きとして入れたい場合は上の `true` を `false` にしてから実行し、
--    文面を直したあとで公開する:
--      UPDATE public.announcements SET published = true
--       WHERE title = '支払方法ごとに集金を記録できるようになりました';
--
-- 4) 取り消す
--      DELETE FROM public.announcements
--       WHERE title = '支払方法ごとに集金を記録できるようになりました';
-- =====================================================================
